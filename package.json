{
  "name": "@lolly/mobile",
  "version": "1.0.1",
  "license": "MPL-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:android": "tauri android dev",
    "dev:ios": "tauri ios dev",
    "dev:frontend": "vite",
    "build:frontend": "npm --prefix ../.. run build:ort && npm --prefix ../.. run build:info && vite build && node -e \"const fs=require('node:fs');for(const f of fs.readdirSync('../web/src/locales'))if(f.endsWith('.json'))fs.rmSync('dist/info/'+f.slice(0,-5),{recursive:true,force:true})\"",
    "build:android": "npm run build:frontend && tauri android build",
    "build:ios": "npm run build:frontend && tauri ios build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-fs": "^2",
    "@tauri-apps/plugin-http": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^5"
  }
}
