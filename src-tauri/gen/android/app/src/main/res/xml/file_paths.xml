<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
  <external-path name="my_images" path="." />
  <cache-path name="my_cache_images" path="." />
  <!-- App-specific external files dir: tauri-plugin-fs BaseDirectory.Download resolves to
       getExternalFilesDir(null)/Download on Android, where the export override saves.
       Exposed so MainActivity's share sheet can mint content:// URIs for those files. -->
  <external-files-path name="exports" path="Download" />
</paths>
