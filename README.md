# This repository has moved

On 2026-09-11 `lolly-mobile` was folded into the main Lolly repository and archived. Its content now lives at:

    https://github.com/lolly-tools/lolly/tree/main/shells/tauri-mobile

The last commit imported from here was `98a5bf0026ec7a02b9d4731dc269b6d01272c3b7`. Every commit of this repository's history was carried across with `git subtree add`, so `git log --all --full-history -- <file>` in the main repository finds it; a bundle of this repository as it stood on that day is kept by the maintainer.

This repository stays public and read-only so that existing clone URLs, issue links and blame permalinks keep resolving. Please open new issues and pull requests on https://github.com/lolly-tools/lolly.
